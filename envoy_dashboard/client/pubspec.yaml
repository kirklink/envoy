name: envoy_dashboard_client
publish_to: none
description: Envoy agent dashboard — Trellis UI.
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  swoop:
    path: ../../../swoop/swoop
  web: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
