name: souvenir
version: 0.1.0
publish_to: none
description: Persistent, human-modeled memory system for autonomous agents.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cellar:
    path: ../../cellar
  ulid: ^2.0.1

dev_dependencies:
  anthropic_sdk_dart: ^0.3.1
  test: ^1.25.0
