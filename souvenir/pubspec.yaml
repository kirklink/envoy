name: souvenir
version: 0.1.0
publish_to: none
description: Persistent, human-modeled memory system for autonomous agents.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  sqlite3: ^3.1.6
  ulid: ^2.0.1

dev_dependencies:
  anthropic_sdk_dart: ^0.3.1
  lints: ^5.1.0
  test: ^1.25.0
